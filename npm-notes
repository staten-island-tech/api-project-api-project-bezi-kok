npm -v                         - check current npm version

npm install -g npm@latest      - install the latest npm version

npm run build                  - IDK

npm i parcel-bundler --save-dev   -builds the node-modules

node -v                        - check current node version